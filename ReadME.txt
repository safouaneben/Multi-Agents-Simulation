##pour compiler les tests il faut taper :

    * make testManager : pour lancer le test Manager qui permet d'afficher le résultat des automates cellulaire et les boids

    * make testBalls : pour lancer le test sur les balls 

##pour executer  les tests compilés,il faut taper:

make exeManager

make exeBalls